const app = () => {
    form()
}

const form = () => {
    const app = document.querySelector('.app')
    const butao = document.createElement('button')
    app.appendChild(butao)
}

const layout = () => {
    const container = document.createElement('container')


    app.appendChild(butao)
}

app()

